# [Implementation 0]
# Installable Python package.
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "local-cloud-model"
version = "1.0.0"
description = "Deterministic multi-tenant cloud state model"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["local_cloud_model"]
