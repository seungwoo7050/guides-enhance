# [Implementation 0]
# Installable package boundary
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "verified-algorithms"
version = "1.0.0"
description = "Deterministic implementations of core data-structure, optimization, graph, and string algorithms."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
