services:
  app:
    environment:
      DB_PASSWORD_FILE: /run/secrets/wrong_db_password
      DB_CONNECT_ATTEMPTS: "4"
      DB_CONNECT_DELAY_MS: "100"
    secrets:
      - wrong_db_password
secrets:
  wrong_db_password:
    file: ./secrets/wrong_db_password.txt
