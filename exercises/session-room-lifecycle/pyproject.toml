[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "session-room-lifecycle"
version = "1.0.0"
description = "Deterministic connection, session, room, and match lifecycle simulator"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "seungwoo7050"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
