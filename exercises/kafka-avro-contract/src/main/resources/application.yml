# [Implementation 3] 직렬화 방식·topic·수동 acknowledgement 설정
# 양쪽 serializer와 topic을 맞추고 처리 성공 뒤에만 offset을 확정합니다.
spring:
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.ByteArraySerializer
      acks: all
      properties:
        enable.idempotence: true
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.ByteArrayDeserializer
      enable-auto-commit: false
      auto-offset-reset: earliest
    listener:
      ack-mode: manual_immediate
guide:
  kafka:
    publish-topic: guide.task.submitted.v1
    consume-topic: guide.task.submitted.v1
    group-id: guide-task-consumer
